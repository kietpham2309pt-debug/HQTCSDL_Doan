<UserControl x:Class="Doan.View.UC_ThongBao"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Doan.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="750"
             d:DesignWidth="1300"
             Background="{DynamicResource BrushBackground}">
    <UserControl.DataContext>
        <vm:ThongBao_VM/>
    </UserControl.DataContext>

    <Border Background="{DynamicResource BrushCard}" CornerRadius="10" Margin="15" Padding="15">
        <DockPanel>
            <Grid DockPanel.Dock="Top" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔔" FontSize="20" Margin="0,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="ĐƠN ĐẶT HÀNG CHỜ XỬ LÝ" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource BrushText}" VerticalAlignment="Center"/>
                    <Border Background="{DynamicResource BrushDanger}" CornerRadius="11" MinWidth="22" Height="22" Margin="10,0,0,0" Padding="6,0" VerticalAlignment="Center">
                        <TextBlock Text="{Binding SoDonCho}" Foreground="White" FontWeight="Bold" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </StackPanel>
                <Button Grid.Column="1" Content="Làm mới" Width="90" Height="32" Background="{DynamicResource BrushPrimary}" Foreground="White" BorderThickness="0" Cursor="Hand" Command="{Binding LenhLamMoi}"/>
            </Grid>

            <!-- Khi không có đơn chờ -->
            <TextBlock DockPanel.Dock="Top" Text="Hiện không có đơn đặt hàng nào chờ xử lý."
                       Foreground="{DynamicResource BrushTextMuted}" FontStyle="Italic" Margin="5,10,0,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding CoDonCho}" Value="False">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding DanhSachDonCho}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#FFFDF5" BorderBrush="#F1C40F" BorderThickness="1" CornerRadius="8" Padding="15" Margin="0,0,0,12">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0" Margin="0,0,15,0">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding TenKhachHang}" FontWeight="Bold" FontSize="15" Foreground="{DynamicResource BrushText}"/>
                                            <TextBlock Text="{Binding SDT, StringFormat=  •  {0}}" FontSize="13" Foreground="{DynamicResource BrushTextMuted}" VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding NgayLap, StringFormat=  •  {0:dd/MM/yyyy HH:mm}}" FontSize="12" Foreground="{DynamicResource BrushTextMuted}" VerticalAlignment="Center"/>
                                        </StackPanel>
                                        <TextBlock Text="{Binding DiaChi, StringFormat=Địa chỉ: {0}}" FontSize="12" Foreground="{DynamicResource BrushTextMuted}" TextWrapping="Wrap" Margin="0,3,0,0"/>
                                        <TextBlock Text="{Binding MoTaMatHang}" FontSize="13" Foreground="#34495E" TextWrapping="Wrap" Margin="0,6,0,0"/>
                                        <TextBlock Text="{Binding TongTien, StringFormat=Tổng tiền: {0:N0} đ}" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource BrushDanger}" Margin="0,6,0,0"/>
                                    </StackPanel>

                                    <Button Grid.Column="1" Content="Xử lý đơn" Width="120" Height="45" VerticalAlignment="Center"
                                            Background="{DynamicResource BrushSuccess}" Foreground="White" FontWeight="Bold" FontSize="14" BorderThickness="0" Cursor="Hand"
                                            Command="{Binding DataContext.LenhXuLyDon, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </DockPanel>
    </Border>
</UserControl>
