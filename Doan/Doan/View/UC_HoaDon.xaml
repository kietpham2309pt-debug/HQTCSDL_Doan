<UserControl x:Class="Doan.View.UC_HoaDon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:Doan.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="750"
             d:DesignWidth="1300"
             Background="#F5F5F5">
    <UserControl.DataContext>
        <vm:HoaDon_VM/>
    </UserControl.DataContext>

    <Border Background="White" CornerRadius="10" Margin="15" Padding="15">
        <DockPanel>
            <TextBlock DockPanel.Dock="Top" Text="LỊCH SỬ GIAO DỊCH" FontSize="18" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15"/>

            <!-- Thanh tìm kiếm + thẻ tổng quan -->
            <Grid DockPanel.Dock="Top" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBox Width="240" Height="32" VerticalContentAlignment="Center" Padding="6,0" Margin="0,0,5,0"
                             Text="{Binding TuKhoaTimKiem, UpdateSourceTrigger=PropertyChanged}"/>
                    <Button Content="Tìm kiếm" Width="90" Height="32" Background="#34495E" Foreground="White" BorderThickness="0" Cursor="Hand" Margin="0,0,5,0" Command="{Binding LenhTimKiem}"/>
                    <Button Content="Làm mới" Width="90" Height="32" Background="#95A5A6" Foreground="White" BorderThickness="0" Cursor="Hand" Command="{Binding LenhLamMoi}"/>
                </StackPanel>

                <Border Grid.Column="1" Background="#EAF2F8" CornerRadius="6" Padding="14,6" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="Tổng số giao dịch" FontSize="11" Foreground="#5D6D7E"/>
                        <TextBlock Text="{Binding TongSoHoaDon}" FontSize="18" FontWeight="Bold" Foreground="#2980B9"/>
                    </StackPanel>
                </Border>
                <Border Grid.Column="2" Background="#FDEDEC" CornerRadius="6" Padding="14,6">
                    <StackPanel>
                        <TextBlock Text="Tổng doanh thu" FontSize="11" Foreground="#922B21"/>
                        <TextBlock Text="{Binding TongDoanhThu, StringFormat={}{0:N0} đ}" FontSize="18" FontWeight="Bold" Foreground="#E74C3C"/>
                    </StackPanel>
                </Border>
            </Grid>

            <DataGrid ItemsSource="{Binding DanhSachHoaDonHienThi}"
                      AutoGenerateColumns="False"
                      Background="Transparent"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      GridLinesVisibility="Horizontal"
                      BorderThickness="0.5"
                      RowHeight="35">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã HĐ" Width="80" Binding="{Binding MaHD}"/>
                    <DataGridTextColumn Header="Ngày lập" Width="100" Binding="{Binding NgayLap, StringFormat=dd/MM/yyyy}"/>
                    <DataGridTextColumn Header="Nhân viên" Width="140" Binding="{Binding TenNhanVien}"/>
                    <DataGridTextColumn Header="Khách hàng" Width="130" Binding="{Binding TenKhachHang}"/>
                    <DataGridTextColumn Header="SĐT" Width="110" Binding="{Binding SDT}"/>
                    <DataGridTextColumn Header="Tên DV/SP" Width="*" Binding="{Binding TenDV_SP}"/>
                    <DataGridTextColumn Header="SL" Width="50" Binding="{Binding SoLuong}"/>
                    <DataGridTextColumn Header="Thành tiền" Width="120" Binding="{Binding ThanhTien, StringFormat={}{0:N0}}"/>
                    <DataGridTextColumn Header="PT thanh toán" Width="110" Binding="{Binding PhuongThucThanhToan}"/>
                    <DataGridTextColumn Header="Trạng thái" Width="110" Binding="{Binding TrangThai}"/>
                </DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Border>
</UserControl>
